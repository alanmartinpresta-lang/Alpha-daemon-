# Alpha True Self-Edit Run 2

Alpha's own `aire/` source tree was searched for editable code locations.
Multiple independent candidate versions were created and fully compiled.
A short real Genesis runtime test was attempted on the baseline and
candidate versions.

No candidate is automatically adopted from compilation or a single short
run. A proper adoption requires a reproducible improvement on held-out
runs.
