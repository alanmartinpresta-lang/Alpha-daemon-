# Missing files for Alpha-daemon-

Copy the contents of this archive into the existing repository, preserving
paths.

These files fill the runtime/Internet/self-evaluation pieces that are absent
from the current GitHub tree.

The `aire/alpha_world.py` file is an adapter to the repository's existing
`ALPHA_GRID_WORLD.py`; it is intentionally not presented as the larger AIRE
physical-world implementation.
