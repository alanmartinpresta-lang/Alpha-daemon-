# Alpha True Self-Edit Run 5 — Reflection Memory

The package adds a reflection memory that records:
state, action, prediction, observed outcome, prediction error, reward,
strategy, and step.

The diagnostic layer can identify recurring prediction instability, negative
return, and action collapse. The included run is a synthetic wiring test;
the next step is connecting these records to the real Alpha decision loop.
